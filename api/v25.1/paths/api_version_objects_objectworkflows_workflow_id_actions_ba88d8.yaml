parameters:
- name: 'workflow_id'
  in: path
  required: true
  description: The workflow id field value.
  schema:
    type: string
get:
  summary: Retrieve Workflow Actions
  parameters:
  - name: loc
    in: query
    required: false
    description: 'When localized (translated) strings are available, retrieve them
      by including loc=true.

      '
    schema:
      type: string
  - name: Authorization
    in: header
    required: false
    example: '{{sessionId}}'
    schema:
      type: string
  - name: Accept
    in: header
    required: false
    example: application/json
    schema:
      type: string
  - name: X-VaultAPI-ClientID
    in: header
    required: false
    description: Include a Client ID to identify this request. This ID appears
      in the API Usage Logs, which is avaiable to download from Admin > Logs >
      API Usage Logs or through the Vault REST API with the Download Daily API
      Usage request. If omitted, the value will appear as `unknown` in the API
      Usage Log.
    example: '{{clientId}}'
    schema:
      type: string
  - name: workflow_id
    in: path
    required: true
    description: The workflow_id parameter.
    schema:
      type: string
  - name: version
    in: path
    required: true
    description: The version parameter.
    schema:
      type: string
  responses:
    '200':
      description: Success
  description: No description available.
  operationId:
    api_version_objects_objectworkflows_workflow_id_actions_ba88d8_get
  tags:
  - Default
